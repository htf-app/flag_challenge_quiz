<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/incorrect_option</item>
        <item name="colorPrimaryDark">@color/incorrect_option</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <!-- Toolbar Style -->
    <style name="CustomToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/incorrect_option</item>
        <item name="android:titleTextColor">@android:color/white</item>
        <item name="android:subtitleTextColor">@android:color/white</item>
        <item name="android:elevation">4dp</item>
        <item name="titleTextAppearance">@style/ToolbarTitleText</item>
    </style>

    <!-- Text Appearance for Toolbar Title -->
    <style name="ToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
